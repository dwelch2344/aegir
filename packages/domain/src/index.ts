/** Identity entity — owned by IAM service (services/iam) */
export type { Identity, IdentityType } from './identity.js'
/** Integration (catalog) entity — owned by System service (services/system) */
export type { SystemIntegration } from './integration.js'
/** Organization entity — owned by IAM service (services/iam) */
export type { Organization } from './organization.js'
/** Tenant entity — owned by System service (services/system) */
export type { Tenant } from './tenant.js'
/** TenantIntegration entity — owned by System service (services/system) */
export type { TenantIntegration, TenantIntegrationStatus } from './tenant-integration.js'
